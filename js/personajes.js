import { bronn, daenerys, jaime, joffrey, tyrion } from "./index.js";

export function createLista() {
  const ListaPersonajes = [joffrey, daenerys, jaime, tyrion, bronn];
  return ListaPersonajes;
}
